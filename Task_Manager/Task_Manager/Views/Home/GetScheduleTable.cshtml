
@{
    ViewData["Title"] = "GetScheduleTable";
}
@model List<List<string>>;
<h2>GetScheduleTable</h2>

<table>
    @for (int i = 0; i < Model.Count; i++)
    {
    <tr>
        @for (int j = 0; j < Model[i].Count; j++)
        {
            <td>@Model[i][j]</td>
        }
    </tr>
    }
</table>

